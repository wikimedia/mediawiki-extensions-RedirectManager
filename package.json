{
	"name": "mediawiki-redirect-manager",
	"private": true,
	"scripts": {
		"test": "npm run test:scripts && npm run test:styles && npm run test:i18n",
		"test:scripts": "eslint --cache .",
		"test:styles": "stylelint --cache **/*.{css,less}",
		"test:i18n": "banana-checker i18n/"
	},
	"devDependencies": {
		"eslint-config-wikimedia": "0.32.4",
		"grunt-banana-checker": "0.13.0",
		"stylelint-config-wikimedia": "0.19.2"
	}
}
